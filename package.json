{
    "name": "web3",
    "version": "1.0.0",
    "main": "index.js",
    "repository": "git@github.com:lucemans/web3-auth-slides.git",
    "author": "Lucemans <luc@lucemans.nl>",
    "license": "GPL3",
    "private": true,
    "scripts": {
      "build": "pnpm -r --paralell exec pnpm build",
      "copy": "rm -rf ./dist && mkdir ./dist && pnpm -r exec pnpm copy"
    }
}
